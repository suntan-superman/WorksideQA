appId: com.workside.sageset
name: SageSet unauthorized member management prevention
tags:
  - sageset
  - phase4
  - negative-path
  - authorization
  - emulator-only
---
- launchApp:
    clearState: true
    clearKeychain: true
- extendedWaitUntil:
    visible:
      id: screen.auth.welcome
    timeout: 30000
- tapOn:
    id: auth.welcome.login
- extendedWaitUntil:
    visible:
      id: screen.auth.login
    timeout: 15000
- tapOn:
    id: auth.login.email
- inputText:
    text: ${SAGESET_MAESTRO_USER_B_EMAIL}
    label: Enter deterministic QA User B email
- tapOn:
    id: auth.login.password
- inputText:
    text: ${SAGESET_MAESTRO_USER_B_PASSWORD}
    label: Enter deterministic QA User B password
- hideKeyboard
- tapOn:
    id: auth.login.submit
    enabled: true
- runFlow:
    when:
      visible: "Not Now"
    commands:
      - tapOn: "Not Now"
- extendedWaitUntil:
    visible:
      id: screen.today.ready
    timeout: 45000
- tapOn:
    id: tab.challenges
- extendedWaitUntil:
    visible:
      id: screen.challenges.ready
    timeout: 45000
- tapOn:
    id: challenges.groups.open
- extendedWaitUntil:
    visible:
      id: screen.social-challenges.ready
    timeout: 45000
- scrollUntilVisible:
    element:
      id: social.group.maestro-group
    direction: DOWN
    timeout: 30000
    visibilityPercentage: 60
- tapOn:
    id: social.group.maestro-group.members
- scrollUntilVisible:
    element:
      id: social.member.maestro_a.row
    direction: DOWN
    timeout: 30000
    visibilityPercentage: 80
- assertVisible:
    id: social.member.maestro_a.row
- assertVisible:
    id: social.member.maestro_a.status
- assertVisible:
    text: '^Creator$'
- assertNotVisible:
    id: social.member.maestro_a.pause
- assertNotVisible:
    id: social.member.maestro_a.resume
- assertNotVisible:
    id: social.member.maestro_a.remove
